<p> <%=@response_JSON['list']['item'][0]['name']%></p>
<ul><% for i in 0..@nutrientHash.length-1%>
     
	<li><%="group is" + @group + @nutrientHash[i]["name"] + " has "+ @nutrientHash[i]["calories"]+ " calories"%> </li> 
    <%end%>

</ul>
<%= form_tag("/search", method: "get") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>


<% end %>
